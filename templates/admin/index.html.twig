{% extends 'base.html.twig' %}

{% block content %}
<table class="table">
    <thead>
        <tr>
            <th>id</th>
            <th>libelle</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {% for service in services %}
        <tr>
            <td>{{service.id}}</td>
            <td>{{service.libelle}}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
         {% endblock %}


